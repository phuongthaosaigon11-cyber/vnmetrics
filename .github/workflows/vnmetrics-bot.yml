name: VNMetrics Super Bot

on:
  schedule:
    - cron: '30 2 * * *' # 09:30 AM VN
  workflow_dispatch: # Cho phép chạy tay

permissions:
  contents: write

jobs:
  auto-scrape:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # QUAN TRỌNG: Nâng lên Node 20 để hỗ trợ thư viện mới
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: npm install axios cheerio --no-save

      - name: Run Scraper Script
        run: node scripts/scrape-etf.cjs

      # Tự động commit file nếu có dữ liệu mới
      - name: Auto Commit Data
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "📊 Auto-update ETF Data"
          file_pattern: 'public/etf_data.json'
          skip_fetch: true
