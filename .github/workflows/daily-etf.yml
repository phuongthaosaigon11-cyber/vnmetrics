name: Daily ETF Update

on:
  schedule:
    # 02:30 UTC = 09:30 Vietnam (Giờ đẹp để có dữ liệu chốt phiên Mỹ)
    - cron: '30 2 * * *'
  workflow_dispatch: # Cho phép bấm nút chạy thủ công để test ngay

jobs:
  update-etf:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Cấp quyền ghi file vào repo
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install axios cheerio

      - name: Run Scraper Script
        run: node scripts/scrape-etf.js

      - name: Commit and Push changes
        run: |
          git config --global user.name 'VNMetrics Bot'
          git config --global user.email 'bot@vnmetrics.io'
          git add public/etf_data.json
          # Chỉ commit nếu có thay đổi dữ liệu mới
          git diff --quiet && git diff --staged --quiet || (git commit -m "📊 Auto-update ETF Data" && git push)
